builds:
  - binary: trusttrack
    dir: ./cmd/trusttrack
    main: main.go
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows

release:
  github:
  prerelease: auto
