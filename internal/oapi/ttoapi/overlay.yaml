overlay: 1.0.0

info:
  title: Basic Overlay for TrustTrack API
  version: 1.0.0

actions:
  - target: $.paths
    description: "Skip code generation for requests"
    remove: true

  - target: $.components.responses
    description: "Skip code generation for responses"
    remove: true

  - target: $.components.securitySchemes
    description: "Skip code generation for security schemes"
    remove: true

  - target: $..[?(@.format == "uuid")].format
    remove: true

  - target: $..[?(@.type=="array")]
    description: "Skip pointer to Go slices"
    update:
      x-go-type-skip-optional-pointer: true

  - target: $..[?(@.format == "email")].format
    description: "Skip email format"
    remove: true

  - target: $.components.schemas.DeviceInputs.properties.canbus_hours_to_service
    description: "Fix canbus_hours_to_service type from number to string (API returns string)"
    update:
      type: string
